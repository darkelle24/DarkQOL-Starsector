{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Package",
            "type": "shell",
            "command": "python ./.vscode/build.py",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$tsc"],
            "detail": "Compile Java source files into the bin directory, create a JAR, and then package it into a RAR file placed in the parent directory.",
        }
        
    ]
}